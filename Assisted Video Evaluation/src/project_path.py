import os

project_path = os.path.abspath('..')
src_folder_path = os.path.join(project_path, 'src')
data_folder_path = os.path.join(project_path, 'data')
saved_images_folder_path = os.path.join(project_path, 'saved images')
